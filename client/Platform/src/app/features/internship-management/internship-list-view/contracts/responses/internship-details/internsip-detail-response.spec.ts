import { InternsipDetailResponse } from './internsip-detail-response';

describe('InternsipDetailResponse', () => {
  it('should create an instance', () => {
    expect(new InternsipDetailResponse()).toBeTruthy();
  });
});
