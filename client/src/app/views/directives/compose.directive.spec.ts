import { ComposeDirective } from './compose.directive';

describe('ComposeDirective', () => {
  it('should create an instance', () => {
    const directive = new ComposeDirective();
    expect(directive).toBeTruthy();
  });
});
